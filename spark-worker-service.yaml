apiVersion: v1
kind: Service
metadata:
  name: spark-worker-service
  labels:
    app: spark
    component: worker
spec:
  selector:
    app: spark
    component: worker
  type: NodePort
  ports:
  - name: worker-web
    port: 8081
    targetPort: 8081
    nodePort: 30081
